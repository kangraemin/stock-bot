# Stock-Bot Project Rules

## 결과 보고 규칙

- 백테스트 결과를 사용자에게 말할 때 **거래 횟수(B/S)를 반드시 포함**한다.
- 수익률, MaxDD, 거래횟수는 항상 세트로 보고한다.

## 실험 → 결과 저장 패턴

백테스트나 분석 실험을 실행한 후 반드시 결과를 저장한다.

### 파일 구조
- 백테스트 코드: 프로젝트 루트 (`*_backtest.py`)
- 결과 CSV: `results/`
- 분석 문서: `analysis/backtest/YYYY-MM-DD-<주제>.md`
- 인덱스: `analysis/INDEX.md`

### 분석 문서 포맷
```markdown
# 제목

- 날짜: YYYY-MM-DD
- 백테스트 코드: `파일명.py`
- 결과 CSV: `results/파일명.csv`

## 개요
한 줄 설명

## 파라미터 / 그리드
테스트한 파라미터 범위

## 결과 요약
핵심 수치 테이블

## 핵심 인사이트
번호 매긴 발견사항 (데이터 근거 포함)

## 한계점
알려진 제약

## 후속 방향
다음에 시도할 것
```

### 인덱스 업데이트
`analysis/INDEX.md` 테이블에 한 줄 추가. 형식:
```
| 날짜 | 카테고리 | 제목 | 파일링크 | 핵심 결과 한 줄 |
```

## 데이터

- 주가 데이터: `data/*.parquet` (yfinance, `download.py`로 다운로드)
- 매크로 데이터: `data/` (HG=F, CL=F, ^VIX, GC=F, ^TNX, DX-Y.NYB)

## 제안 규칙

- 새로운 전략/실험을 "할까요?", "해볼까요?", "시작할까요?" 등으로 제안할 때, **반드시 전략 상세를 먼저 설명**한다:
  - 매수/매도 조건 (구체적 수식/임계값)
  - 그리드 서치 파라미터 범위
  - 대상 종목/데이터
  - 기존 전략 대비 변경점
- 전략 설명 없이 "해볼까요?"만 던지지 않는다.

## 데이터로 말하기 규칙

- **투자 전략을 제안할 때 반드시 백테스트를 먼저 돌린다.** 백테스트 결과 없이 전략을 제안하지 않는다.
- 백테스트 결과가 기준 미달이면 가설을 수정하고 다시 돌린다. 괜찮은 결과가 나올 때까지 반복한다.
  - 기준: OOS Sharpe > 0.5, trades >= 10, Walk-Forward 승률 > 60%
- 결과가 나오면 수치(Sharpe, MDD, trades, trades/yr, vs B&H)와 함께 제안한다.
- "~할 것 같다", "~일 수 있다" 같은 추측 금지. 데이터로 보여준다.

## 알림봇

- `alert.py`: 멀티 종목 텔레그램 알림 (cron 매일 KST 06:00)
- 배포: Oracle Cloud (ubuntu@158.179.166.232), ~/stock-bot/
- 상태 파일: `.states/*.json`
